[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["backend/**"]

[deploy]
startCommand = "celery -A config worker -l info --concurrency=${CELERY_CONCURRENCY:-1}"
restartPolicyType = "ON_FAILURE"
